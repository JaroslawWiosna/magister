dist: trusty
os: linux
language: cpp

compiler:
    - gcc
    - clang

before_script:
    - echo "before_script"

script: 
    - cmake -H. -B_builds && (cd _builds/ && make clean && make -j1 && make test)

after_success:
    - echo "success!"
